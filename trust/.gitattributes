# 这两个文件的字节是被签名保护的：trustlist.sig 签的是 trustlist.json 的**确切字节**。
# 任何行尾转换都会让 GitHub 吐出来的内容与签名时的内容不是同一份，症状是所有用户
# 机器同时报「清单不可信」，而排查方向会被指向发布侧或中间人——真正的原因却是
# git 在 checkout 时改了字节。
#
# -text 关闭一切行尾归一化。这里必须用它，而不是 eol=lf：eol=lf 只统一工作区，
# 而我们要的是「工作区、git blob、raw 服务的字节三者恒等」。
*.json -text
*.sig  -text
